#!/usr/bin/python
#coding=utf-8


def main():
    #元组与列表类似，不同在于元组的元素不能修改


    ########
    ##元组内置函数
    ########
    '''
    cmp(tuple1, tuple2)
        比较两个元组元素。
    len(tuple)
        计算元组元素个数。
    max(tuple)
        返回元组中元素最大值。
    min(tuple)
        返回元组中元素最小值。
    tuple(seq)
        将列表转换为元组。
    '''

if __name__ == "__main__":
    main()
